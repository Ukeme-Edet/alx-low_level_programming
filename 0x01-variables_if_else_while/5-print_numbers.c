#include <stdio.h>

/**
 * main - The program entry point
 * Return: 0 if the program is successful
 */
int main(void)
{
	int i = 0;

	while (i < 10)
		printf("%d", i++);
	printf("\n");
	return (0);
}
